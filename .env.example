# MyApp Store 환경 변수 설정 예시
# 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 수정하세요.
# cp .env.example .env

# ===========================================
# 네트워크 설정
# ===========================================

# 프론트엔드 URL 설정
# localhost: 호스트 머신에서만 접속 가능
# 192.168.0.X: 같은 네트워크의 다른 기기에서도 접속 가능
VITE_API_BASE_URL=http://192.168.0.8:8100/api
VITE_BACKEND_URL=http://192.168.0.8:8100
VITE_APP_URL=http://192.168.0.8:5900

# ===========================================
# 보안 설정
# ===========================================

# JWT 시크릿 키 (반드시 변경하세요!)
SECRET_KEY=your-super-secret-key-please-change-this-in-production

# ===========================================
# AI 설정 (선택사항)
# ===========================================

# OpenAI API 키
OPENAI_API_KEY=

# Google Gemini API 키
GEMINI_API_KEY=

# ===========================================
# 이메일 설정 (사용자 초대 기능용, 선택사항)
# ===========================================

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=MyApp Store

# ===========================================
# CORS 설정
# ===========================================

# 개발/내부 NAS 환경: "*"로 모든 origin 허용
# 프로덕션: 구체적인 도메인 지정
# 예: http://192.168.0.8:5900,https://myapp.example.com
CORS_ORIGINS=*

# ===========================================
# 사용 가이드
# ===========================================

# 1. 로컬 네트워크 접속 설정:
#    - 서버 IP 확인: ip addr show 또는 hostname -I
#    - 위의 192.168.0.8을 실제 IP로 변경
#    - docker-compose up -d로 재시작
#
# 2. 역방향 프록시 사용 시:
#    - NPM이나 Synology 역방향 프록시 설정
#    - HTTPS URL을 CORS_ORIGINS에 추가
#    - 예: CORS_ORIGINS=https://myapp.example.com
#
# 3. 더 자세한 설정 방법은 NETWORK_SETUP.md 참고
