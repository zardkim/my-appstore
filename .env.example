# MyApp Store 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# ==================== 데이터베이스 설정 ====================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=myappstore
POSTGRES_PORT=5433  # Synology 기본 PostgreSQL과 충돌 방지

# ==================== Redis 설정 ====================
# 내부 NAS 환경에서는 비밀번호 없이 사용 가능 (빈 값)
# 외부 접속이 필요한 경우 비밀번호 설정 권장
REDIS_PASSWORD=
REDIS_PORT=6380  # 충돌 방지

# ==================== 보안 설정 ====================
# 중요: 강력한 SECRET_KEY를 생성하세요
# 생성 방법: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== CORS 설정 ====================
# 개발/내부 NAS 환경: "*" 사용 (모든 origin 허용)
# 프로덕션/역방향 프록시 사용 시: 실제 도메인 추가 권장
# 예: https://app.nuripc.kr,http://192.168.0.15:5900
CORS_ORIGINS=*

# ==================== 서버 포트 ====================
BACKEND_PORT=8110
FRONTEND_PORT=5900

# ==================== 로깅 설정 ====================
LOG_LEVEL=INFO
ENVIRONMENT=development

# ==================== AI 설정 (선택사항) ====================
# 메타데이터 자동 생성을 위한 AI API 키
# OpenAI 또는 Gemini 중 하나만 설정해도 됩니다
GEMINI_API_KEY=
OPENAI_API_KEY=

# ==================== 시놀로지 폴더 마운트 (선택사항) ====================
# docker-compose.yml의 backend > volumes 섹션에서 설정
# 시놀로지의 기존 소프트웨어 폴더를 앱 스토어에 표시하려면:
# 1. docker-compose.yml 파일 열기
# 2. backend > volumes 섹션에서 주석 해제:
#    - /volume2/App:/app/data/library/App:ro
# 3. :ro는 읽기 전용(권장), 제거하면 읽기/쓰기 가능
# 4. docker-compose down && docker-compose up -d 실행

# ==================== 프론트엔드 환경변수 (선택사항) ====================
# 기본값: 상대 경로 (/api) - 역방향 프록시 환경에서 자동 작동
#
# 1. 역방향 프록시 사용 시 (Nginx, Caddy, Synology 등) - 설정 불필요
#    기본값을 그대로 사용하면 https://your-domain.com/api로 자동 연결됩니다
#
# 2. 로컬 네트워크에서 직접 접속 (역방향 프록시 없이)
#    실제 NAS IP 주소로 설정하세요:
# VITE_API_BASE_URL=http://192.168.0.100:8110/api
# VITE_BACKEND_URL=http://192.168.0.100:8110
# VITE_APP_URL=http://192.168.0.100:5900
