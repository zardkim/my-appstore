# MyApp Store 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# ==================== 데이터베이스 설정 ====================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=myappstore
POSTGRES_PORT=5433  # Synology 기본 PostgreSQL과 충돌 방지

# ==================== Redis 설정 ====================
# 내부 NAS 환경에서는 비밀번호 없이 사용 가능 (빈 값)
# 외부 접속이 필요한 경우 비밀번호 설정 권장
REDIS_PASSWORD=
REDIS_PORT=6380  # 충돌 방지

# ==================== 보안 설정 ====================
# 중요: 강력한 SECRET_KEY를 생성하세요
# 생성 방법: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== CORS 설정 ====================
# 개발/내부 NAS 환경: "*" 사용 (모든 origin 허용)
# 프로덕션: 실제 도메인으로 변경하세요
# 예: http://myapp.example.com:5900,https://myapp.example.com
CORS_ORIGINS=*

# ==================== 서버 포트 ====================
BACKEND_PORT=8110
FRONTEND_PORT=5900

# ==================== 로깅 설정 ====================
LOG_LEVEL=INFO
ENVIRONMENT=development

# ==================== 프론트엔드 환경변수 (선택사항) ====================
# 기본값: localhost (호스트 머신에서만 접속)
# 다른 기기에서 접속하려면 실제 IP 주소로 변경하세요
# 예: http://192.168.0.100:8110/api
# VITE_API_BASE_URL=http://localhost:8110/api
# VITE_BACKEND_URL=http://localhost:8110
# VITE_APP_URL=http://localhost:5900
